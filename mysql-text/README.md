# プログラミング未経験から始める MySQL

**このテキスト集の5冊目（フェーズ1の最終巻）です。**

> 🚧 **準備中です。**
> [react-text](../react-text/README.md) → [python-text](../python-text/README.md)
> → [fastapi-text](../fastapi-text/README.md) → [docker-text](../docker-text/README.md)
> の順で読んでください。

---

## この本の位置づけ

**データベースは、Web アプリの心臓部です。**

ここまでのテキストでも少しデータベースに触れてきましたが、
この本では**データベースそのもの**を正面から扱います。

- どうやってデータを取り出すのか（SQL）
- どうやってテーブルを設計するのか
- なぜ検索が遅くなるのか、どう直すのか

この本を終えると、**フェーズ1「作れるようになる」が完了**します。
React + FastAPI + MySQL を Docker で動かす Web アプリを、一人で作れる状態です。

## この本の前提

- **docker-text の第2章まで**（Docker で MySQL を起動するため）
- fastapi-text（第8章でアプリから接続します）

---

## 予定している目次

### 第0章 はじめに
- 0.1 このテキストについて
- 0.2 AI サポートの準備
- 0.3 この本で作るもの

### 第1章 データベースとは
- 1.1 ファイルに保存するのでは何がだめなのか
- 1.2 リレーショナルデータベース
- 1.3 テーブル・行・列
- 1.4 主キー
- 1.5 MySQL / PostgreSQL / SQLite の違い

### 第2章 環境構築
- 2.1 Docker で MySQL を起動する
- 2.2 接続する（CLI）
- 2.3 GUI クライアントを使う
- 2.4 データベースとテーブルを作る
- 2.5 練習用データを投入する
- 2.6 **文字コードの設定**（日本語の文字化け対策）

### 第3章 データを取り出す（SELECT）
- 3.1 `SELECT` の基本
- 3.2 `WHERE` で絞り込む
- 3.3 比較演算子と論理演算子
- 3.4 `LIKE` であいまい検索
- 3.5 `IN` / `BETWEEN` / `IS NULL`
- 3.6 `ORDER BY` で並べ替える
- 3.7 `LIMIT` と `OFFSET`
- 3.8 `DISTINCT`
- 3.9 よく使う関数

### 第4章 データを変更する
- 4.1 `INSERT`
- 4.2 `UPDATE`
- 4.3 `DELETE`
- 4.4 **`WHERE` を忘れた事故**
- 4.5 トランザクション
- 4.6 `COMMIT` と `ROLLBACK`

### 第5章 テーブル設計
- 5.1 データ型を選ぶ
- 5.2 `NOT NULL` と デフォルト値
- 5.3 主キーと `AUTO_INCREMENT`
- 5.4 外部キーと参照整合性
- 5.5 `UNIQUE` 制約
- 5.6 正規化の入り口
- 5.7 テーブルを変更する（`ALTER TABLE`）

### 第6章 結合と集計
- 6.1 なぜテーブルを分けるのか
- 6.2 `INNER JOIN`
- 6.3 `LEFT JOIN`
- 6.4 複数テーブルの結合
- 6.5 `GROUP BY` と集計関数
- 6.6 `HAVING`
- 6.7 サブクエリ
- 6.8 多対多と中間テーブル

### 第7章 インデックスと実行計画
- 7.1 なぜ検索が遅くなるのか
- 7.2 インデックスの仕組み
- 7.3 インデックスを張る
- 7.4 `EXPLAIN` の読み方
- 7.5 インデックスが効かないパターン
- 7.6 張りすぎのデメリット

### 第8章 アプリから使う
- 8.1 FastAPI から接続する
- 8.2 SQLAlchemy とリレーション
- 8.3 **N+1 問題**
- 8.4 SQL インジェクション対策
- 8.5 コネクションプール

### 第9章 次のステップ
- 9.1 フェーズ1の完走おめでとうございます
- 9.2 ここまでで身についたこと
- 9.3 これから学ぶべきこと（フェーズ2へ）

### 解答編
