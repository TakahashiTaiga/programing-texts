---
title: "第3章 データを取り出す（SELECT）"
---

# 第3章 データを取り出す（SELECT）

> 🚧 **この章は執筆中です。**
> 以下は予定している章立てです。第2章を読み終えた方は、
> この章が書き上がるまでお待ちください。
>
> 第2章で投入した練習用テーブル（`categories` / `customers` / `products` /
> `orders` / `order_items`）を、この章から使っていきます。

## 予定している章立て

### 3.1 `SELECT` の基本

- 3.1.1 全件取り出す
- 3.1.2 列を指定する
- 3.1.3 アスタリスクを本番で使わない理由
- 3.1.4 別名を付ける（`AS`）

### 3.2 `WHERE` で絞り込む

- 3.2.1 条件を書く
- 3.2.2 比較演算子
- 3.2.3 `AND` / `OR` / `NOT`
- 3.2.4 かっこで優先順位を決める

### 3.3 いろいろな条件

- 3.3.1 `LIKE` であいまい検索
- 3.3.2 `IN` で複数候補
- 3.3.3 `BETWEEN` で範囲
- 3.3.4 `IS NULL` を使う理由

### 3.4 並べ替えと件数制限

- 3.4.1 `ORDER BY`
- 3.4.2 昇順と降順
- 3.4.3 複数キーで並べる
- 3.4.4 `LIMIT` と `OFFSET`

### 3.5 重複を除く

- 3.5.1 `DISTINCT`

### 3.6 よく使う関数

- 3.6.1 文字列関数
- 3.6.2 日付関数
- 3.6.3 数値関数
- 3.6.4 `CASE` で条件分岐

---

→ [第2章 環境構築](./02-environment.md) に戻る
