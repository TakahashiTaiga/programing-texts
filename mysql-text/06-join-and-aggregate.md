---
title: "第6章 結合と集計"
---

# 第6章 結合と集計

> 🚧 **この章は執筆中です。**
> 以下は予定している章立てです。第5章を読み終えた方は、
> この章が書き上がるまでお待ちください。
>
> 第5章では、テーブルを**分ける**理由と方法を扱いました（5.5）。
> 分けたぶん、`loans` には `book_id` と `member_id` という**番号しか入っていません。**
> この章では、**分けたテーブルを1つの結果として取り出す**方法（結合）と、
> **件数や合計を求める**方法（集計）を扱います。
> 第2章 2.5.1 で見せた 15 行の SQL が読めるようになる、この本の山場です。

## 予定している章立て

### 6.1 なぜテーブルを分けるのか

- 6.1.1 1枚の表で持つ問題
- 6.1.2 分けた結果、結合が必要になる

### 6.2 `INNER JOIN`

- 6.2.1 2つのテーブルを繋ぐ
- 6.2.2 `ON` の書き方
- 6.2.3 テーブルに別名を付ける

### 6.3 `LEFT JOIN`

- 6.3.1 片方にしかない行も出す
- 6.3.2 `INNER` との違いを図で理解する
- 6.3.3 `NULL` になる列の扱い
- 6.3.4 **`WHERE` に書くと `INNER` と同じになる罠**

### 6.4 複数テーブルの結合

- 6.4.1 3つ以上を繋ぐ
- 6.4.2 読みやすい書き方

### 6.5 集計

- 6.5.1 集計関数（`COUNT` / `SUM` / `AVG` / `MAX` / `MIN`）
- 6.5.2 `GROUP BY`
- 6.5.3 **`COUNT` の引数による違い**
- 6.5.4 `HAVING` と `WHERE` の使い分け

### 6.6 サブクエリ

- 6.6.1 `WHERE` の中で使う
- 6.6.2 `FROM` の中で使う
- 6.6.3 相関サブクエリ
- 6.6.4 `JOIN` で書き換える

### 6.7 多対多

- 6.7.1 中間テーブル
- 6.7.2 設計と結合の書き方

---

→ [第5章 テーブル設計](./05-table-design.md) に戻る
