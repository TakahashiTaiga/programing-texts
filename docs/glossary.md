# 用語集

全テキストで**同じ言い回し**を使うための辞書。
初出時はここの定義文をベースに書く（そのままコピーしてよい）。

サポート AI もこのファイルを参照して、説明の言い回しを揃えてください。

---

## 共通（コンピュータ・開発全般）

| 用語 | 定義文（初出時にこう書く） | たとえ |
|------|--------------------------|--------|
| プログラム | コンピュータへの指示を、決まった書き方で並べたもの | 料理のレシピ |
| ソースコード | 人間が読み書きするために書いたプログラムの文章そのもの | レシピの紙 |
| 実行する | 書いたプログラムをコンピュータに読ませて、実際に動かすこと | レシピどおりに作る |
| ファイル | 名前を付けて保存された1つのデータのまとまり | — |
| ディレクトリ | ファイルをまとめて入れておく入れ物。Windows の画面上では「フォルダ」と表示される | 引き出し |
| パス | ファイルやディレクトリの場所を表す文字列 | 住所 |
| 絶対パス | 一番上から辿って書いた、場所を一意に決めるパス | 「東京都〜」から書いた住所 |
| 相対パス | いまいる場所を基準にして書いたパス | 「ここから2つ隣」 |
| 拡張子 | ファイル名の末尾に付く `.html` `.js` などの部分。ファイルの種類を表す | — |
| ターミナル | 文字でコンピュータに命令するための画面。Windows では PowerShell、macOS では「ターミナル」 | 受付窓口 |
| コマンド | ターミナルに打ち込む命令 | — |
| エディタ | ソースコードを書くための道具。このテキストでは VS Code を使う | — |
| ライブラリ | 他の人が作った、便利な機能のまとまり。自分のプログラムから呼び出して使う | 出来合いの部品 |
| フレームワーク | アプリの土台となる、大きな作りかけの構造。決められた場所に自分のコードを入れて完成させる | 建物の骨組み |
| パッケージ | 配布できる形にまとめられたライブラリ | 箱に入った部品 |
| 依存関係 | そのプログラムを動かすために必要な、他の人が作った部品のこと | — |
| パッケージマネージャ | ライブラリのインストール・更新・削除を管理する道具。npm や pip がこれにあたる | 部品の発注係 |
| バージョン | ソフトウェアの版。`v22.3.1` のように数字で表す | — |
| バグ | プログラムの間違い。意図どおりに動かない原因 | — |
| デバッグ | バグの原因を見つけて直す作業 | — |
| エラー | 実行中に問題が起きたことをコンピュータが知らせてくるメッセージ | — |
| PATH | ターミナルがコマンドを探しに行く場所の一覧。ここに登録されていないとコマンドが見つからない | 電話帳 |
| 環境変数 | OS 全体やターミナル全体で共有される設定値。PATH もそのひとつ | — |
| 環境構築 | プログラムを書いて動かせる状態を作る準備作業 | — |
| リポジトリ | プログラムのソースコードと、その変更履歴を保管する場所 | — |
| Git | ソースコードの変更履歴を記録・管理する道具 | セーブ機能 |
| GitHub | Git のリポジトリをインターネット上に置いて共有できるサービス | — |

---

## Web

| 用語 | 定義文 | たとえ |
|------|--------|--------|
| ブラウザ | Web ページを見るためのソフト。Chrome、Edge、Safari など | — |
| サーバー | 他のコンピュータからの依頼を受けて、データを返すコンピュータ（またはそのプログラム） | レストランの厨房 |
| クライアント | サーバーに依頼を出す側。ブラウザがこれにあたる | 客席 |
| リクエスト | クライアントからサーバーへの依頼 | 注文 |
| レスポンス | サーバーからクライアントへの返答 | 料理 |
| HTTP | ブラウザとサーバーがやり取りするときの、決められた話し方のルール | 注文の仕方の決まり |
| HTTPS | HTTP の通信内容を暗号化したもの | — |
| URL | インターネット上の場所を表す文字列 | 住所 |
| ドメイン | `example.com` のような、サーバーの名前 | — |
| ポート | 1台のコンピュータの中で、どのプログラムに繋ぐかを表す番号 | 内線番号 |
| localhost | 自分のパソコン自身を指す名前 | — |
| HTML | Web ページの「何がどこにあるか」（構造）を書くための言語 | 建物の骨組み |
| CSS | Web ページの「どう見えるか」（見た目）を指定するための言語 | 内装 |
| JavaScript | Web ページを「動かす」ためのプログラミング言語 | 電気・配線 |
| DOM | ブラウザが HTML を読み込んで、プログラムから触れるように組み立てた部品の集まり | — |
| API | プログラムから他のプログラムの機能を呼び出すための窓口 | 注文カウンター |
| JSON | データをやり取りするための、文字だけで書かれた形式 | — |
| フロントエンド | ユーザーが直接見て触る側。ブラウザで動く部分 | 客席・接客 |
| バックエンド | 裏で動いてデータを処理する側。サーバーで動く部分 | 厨房 |
| マークアップ言語 | 文章に印を付けて、どこが何なのかという意味を持たせる言語 | — |
| スタイルシート言語 | 表示の見た目を指定するための言語 | — |
| 開発者ツール | ブラウザに内蔵されている、表示中のページの中身や通信を調べるための道具 | — |
| SPA | 1枚の HTML の上で、JavaScript が中身を差し替えて画面を切り替えるアプリ | — |
| CORS | 異なる場所からの読み込みを制限する、ブラウザの安全機能 | — |
| localStorage | ブラウザがサイトごとに用意している、文字列を保存しておける小さな保管庫 | — |
| デプロイ | 作ったものを、動かす場所に配置して公開すること | — |
| バージョン管理 | ファイルの変更履歴を記録し、いつでも過去の状態に戻せるようにする仕組み | — |
| HTTP メソッド | そのデータをどう扱ってほしいかを表す動詞。`GET` / `POST` / `PUT` / `PATCH` / `DELETE` など | — |
| ステータスコード | レスポンスの先頭に付く3桁の数字で、依頼がどうなったかを表す | — |
| ヘッダー | リクエストやレスポンスに付ける、データについての説明書き | 宅配便の伝票 |
| ボディ | リクエストやレスポンスで実際にやり取りされるデータ本体 | 宅配便の中身 |
| REST | URL を名詞（対象）、HTTP メソッドを動詞として API を設計する考え方 | — |
| リソース | API が扱う、名前を付けられるデータのまとまり | — |
| curl | ターミナルから HTTP リクエストを送るためのコマンド | — |
| パスパラメータ | URL の一部分として渡す値。「どれか1つ」を指すのに使う | 部屋番号 |
| クエリパラメータ | URL の `?` 以降に「名前=値」の形で付ける値。絞り込みや並べ替えの指定に使う | 検索の条件欄 |
| リクエストボディ | リクエストのボディに載せて送る、登録・更新する中身そのもの | — |
| クッキー | サーバーがブラウザに預けておき、次のリクエストで自動的に送り返してもらう小さな値 | 番号札 |
| パーセントエンコーディング | URL に使えない文字を `%` と数字で表す決まり | — |

---

## JavaScript / React

| 用語 | 定義文 | たとえ |
|------|--------|--------|
| 変数 | 値に名前をつけて、あとから使い回すための仕組み | 名前を書いた箱 |
| 定数 | 一度決めたら変えない値につける名前。`const` で作る | — |
| 型 | 値の種類（数値・文字列など） | — |
| 関数 | よく使う処理に名前を付けて、いつでも呼び出せるようにしたもの | 自動販売機（入れると出てくる） |
| 引数（ひきすう） | 関数に渡す値 | 自販機に入れるお金 |
| 戻り値 | 関数が返してくる結果の値 | 出てくる商品 |
| 配列 | 複数の値を順番に並べて1つにまとめたもの | 番号付きのロッカー |
| オブジェクト | 名前と値の組を、まとめて1つにしたもの | 名札付きの引き出し |
| メソッド | オブジェクトに属している関数 | — |
| 非同期処理 | 結果が返ってくるのを待たずに、先に次の処理を進めるやり方 | 注文して席で待つ |
| Promise | 非同期処理の「あとで結果が来る」という約束を表すもの | 呼び出しベル |
| モジュール | 機能ごとにファイルを分け、必要なものだけ他のファイルから使えるようにする仕組み | — |
| Node.js | ブラウザの外でも JavaScript を動かせるようにしたもの | — |
| npm | Node.js 用のパッケージマネージャ | — |
| React | 画面を「部品」の組み合わせで作るための JavaScript のライブラリ | — |
| コンポーネント | 画面の一部分を、見た目と動きごと1つにまとめた部品 | レゴブロック |
| JSX | JavaScript の中に HTML のような書き方を混ぜられる記法 | — |
| props | 親コンポーネントから子コンポーネントへ渡す値 | 部品に貼るラベル |
| state（状態） | コンポーネントが自分で持っていて、変わると画面が描き直される値 | ホワイトボードの数字 |
| フック | `use` で始まる React の特別な関数。state などの機能を使えるようにする | — |
| レンダリング | React が画面に表示する内容を組み立て直すこと。描画 | — |
| 再レンダリング | state や props が変わったときに、React が画面を作り直すこと | — |
| 副作用 | 画面の組み立て以外の処理（データ取得、タイマー設定など） | — |
| ビルド | 書いたコードを、ブラウザが実行できる形に変換してまとめること | — |
| Vite | React のプロジェクトを作り、開発中の自動反映やビルドを行う道具 | — |
| 真偽値 | 正しいか正しくないかを表す型。`true` と `false` の2つしかない | — |
| スコープ | その変数が使える範囲 | — |
| テンプレートリテラル | バッククォートで囲み、`${ }` で値を埋め込める文字列の書き方 | — |
| 分割代入 | 配列やオブジェクトから、値を一度に複数の変数に取り出す書き方 | — |
| スプレッド構文 | 配列やオブジェクトの中身を、その場に展開する記法 | — |
| イミュータブルな更新 | 元のデータを直接書き換えず、新しいものを作って差し替えるやり方 | — |
| key | React がリストの各要素を見分けるための目印 | 座席番号 |
| 制御コンポーネント | React の state が値を管理している入力欄 | — |
| 依存配列 | `useEffect` などに渡す、いつ実行するかを決める配列 | — |
| クリーンアップ関数 | `useEffect` が返す、後始末をするための関数 | — |
| カスタムフック | 名前が `use` で始まり、中でほかのフックを呼べる、自分で作った関数 | — |
| コンポーネントツリー | 部品の親子関係を木の形で表したもの | 家系図 |
| Context | 途中のコンポーネントを飛び越えて、値を下の階層に届けるための仕組み | 館内放送 |
| エラーバウンダリ | 下の階層で起きたエラーを受け止めて、代わりの表示に差し替える仕組み | — |
| React Router | React で URL と画面の対応づけを行うライブラリ | — |
| TypeScript | JavaScript に「型」の情報を書き足せるようにした言語 | — |

---

## Python / FastAPI

| 用語 | 定義文 |
|------|--------|
| インタプリタ | ソースコードを1行ずつ読んで実行するプログラム。`python` コマンドがこれ |
| REPL | 1行入力するたびに結果が返ってくる対話モード |
| トレースバック | エラーが起きたとき、どこで・どんな問題が起きたかをたどれるように表示される情報 |
| 仮想環境（venv） | プロジェクトごとにライブラリを分けて入れるための隔離された領域 |
| pip | Python 用のパッケージマネージャ |
| インデント | 行頭の字下げ。Python ではこれが処理のまとまりを表す |
| 予約語 | その言語が文法として使うために取ってある単語。変数名には使えない |
| snake_case | 単語を小文字にしてアンダースコアでつなぐ命名の書き方。Python の変数名・関数名で使う |
| 演算子 | `+` や `>` のように、値どうしを計算・比較するための記号 |
| インデックス | 並んでいるものの位置を表す番号。Python では 0 から始まる |
| スライス | `[開始:終了]` の形で、並んでいるものの一部を切り出すこと |
| イミュータブル | 不変。作ったあと中身を変えられない性質（文字列・タプルがこれにあたる） |
| エスケープシーケンス | バックスラッシュ `\` で始まる、特別な意味を持つ書き方 |
| f-string | 文字列の前に `f` を付け、`{ }` の中に値を埋め込める書き方 |
| PEP 8 | Python の公式なコーディング規約。字下げ・空白・命名などの書き方の約束 |
| 制御構文 | 処理の流れを枝分かれさせたり、繰り返したりするための書き方 |
| ガード節 | 処理の本体に入る前に、対象外の場合を先に追い返す書き方 |
| データ構造 | 複数の値を、決まった形でまとめて持つための仕組み |
| リスト | 複数の値を順番に並べたもの（JavaScript の配列にあたる） |
| 要素 | リストやタプルに入っている1つ1つの値 |
| タプル | 複数の値を順番に並べたもので、作ったあと中身を変えられないもの |
| 辞書（dict） | キーと値の組をまとめたもの（JavaScript のオブジェクトにあたる） |
| キー | 辞書から値を取り出すための名前 |
| 集合（set） | 重複しない値の集まり。順番を持たない |
| ミュータブル | 作ったあとから中身を変えられる性質（リスト・辞書・集合がこれにあたる） |
| 破壊的 | 新しいものを作らず、元のデータそのものを書き換えてしまう動きのこと |
| アンパック | まとまった値の中身を取り出して、それぞれの変数に配ること |
| 内包表記 | リストや辞書を、`for` を使って1行で組み立てる書き方 |
| ラムダ式 | 名前を付けずに、その場で作る短い関数 |
| docstring | 関数やクラスの先頭に書く、そのはたらきを説明する文 |
| 標準ライブラリ | Python をインストールした時点で最初から付いてくるモジュールの集まり |
| 文字コード | 文字と、コンピュータが扱う番号との対応表。UTF-8 や cp932 がこれにあたる |
| CSV | 値をカンマで区切って並べた、表形式のテキストファイル |
| クラス | 同じ性質を持つデータと処理をまとめた設計図 |
| インスタンス | クラスという設計図から作られた実物 |
| 属性 | インスタンスが持っているデータ。変数のようなもの |
| 継承 | あるクラスの属性とメソッドを、別のクラスがそのまま引き継ぐこと |
| 親クラス／子クラス | 引き継がれる元のクラスと、引き継ぐ側のクラス |
| オーバーライド | 親クラスと同じ名前のメソッドを子クラスで定義し直し、動きを上書きすること |
| 特殊メソッド | `__str__` のように、Python が決まった場面で自動的に呼び出すメソッド |
| デコレータ | `@` から始まる、その下のクラスや関数にあとから機能を足す印 |
| dataclass | 項目の型を書くだけで `__init__` などを自動生成してくれる仕組み。`@dataclass` と書いて使う |
| 例外 | 実行中に起きた問題を表すオブジェクト。`try` / `except` で受け止める |
| 型ヒント | 変数や関数に「この型のはず」と注記を書く仕組み |
| 型チェッカー | ソースコードを実行せずに読んで、型の矛盾を報告する道具。mypy や pyright がこれ |
| 静的型チェック | プログラムを動かさずに、型が合っているかを調べること |
| リンタ | コードを実行せずに読んで、規約違反や怪しい書き方を指摘する道具 |
| フォーマッタ | コードの空白や改行を、決められた形に自動で整える道具 |
| ruff | リンタとフォーマッタを1つでこなす、Python 用の道具 |
| uv | 仮想環境の作成とパッケージのインストールをまとめて速く行う、新しい道具 |
| TOML | 設定を書くためのファイル形式。`pyproject.toml` で使う |
| 外部ライブラリ | 標準ライブラリには入っておらず、`pip install` して使うライブラリ |
| requests | Python から HTTP リクエストを送るための外部ライブラリ |
| argparse | コマンドラインから渡された引数を受け取るための標準ライブラリ |
| テスト | プログラムが期待どおりに動くかを、別のプログラムで確認すること |
| pytest | Python でテストを書いて実行するための道具 |
| `assert` | 「ここではこうなっているはず」と書いておき、そうでなければ止める文。テストの合否はこれで決まる |
| DRY RUN | 実際には実行せず、やることの計画だけを表示して確かめること |
| pandas | 表形式のデータをまとめて扱うための Python のライブラリ |
| DataFrame | pandas で、行と列を持つ表そのものを表す値 |
| Series | pandas で、表の1列ぶんのデータを表す値 |
| エンドポイント | API のうち、特定の URL とメソッドの組で呼び出せる1つの窓口 |
| ルーティング | どの URL のリクエストをどの処理に渡すかの割り当て |
| バリデーション | 受け取ったデータが正しい形式かを検査すること |
| 依存性注入（DI） | 処理に必要な部品を、外から渡してもらう作り方 |

---

## Docker / MySQL

| 用語 | 定義文 | たとえ |
|------|--------|--------|
| コンテナ | アプリと、その動作に必要な環境をまとめて隔離して動かす仕組み | 中身ごと運べる箱 |
| イメージ | コンテナを作るための設計図兼テンプレート | 設計図・型 |
| Dockerfile | イメージの作り方を書いたファイル | レシピ |
| レイヤ | Dockerfile の1命令ごとに作られる、イメージの層 | — |
| ボリューム | コンテナを消してもデータを残すための保存領域 | 外付けハードディスク |
| バインドマウント | パソコン上のディレクトリを、コンテナの中から見えるようにすること | — |
| Docker Compose | 複数のコンテナの構成をファイルに書いて、まとめて起動する道具 | — |
| データベース | データを整理して保存し、必要なものを素早く取り出せるようにした仕組み | 巨大な表計算ソフト |
| RDB（リレーショナルデータベース） | データを表（テーブル）の形で持ち、表どうしを関連づけて扱うデータベース | — |
| テーブル | 行と列からなる表。データの保存単位 | Excel のシート |
| レコード（行） | テーブルの1行。1件分のデータ | — |
| カラム（列） | テーブルの1列。項目 | — |
| 主キー | その行を1つに特定できる列 | 学籍番号 |
| 外部キー | 別のテーブルの行を指し示す列 | — |
| SQL | データベースに命令を出すための言語 | — |
| トランザクション | 複数の操作を「全部成功」か「全部なかったこと」にまとめる仕組み | — |
| インデックス | 検索を速くするための索引 | 本の索引 |
| マイグレーション | テーブルの構造の変更を、記録して順番に適用する仕組み | — |

---

## 更新ルール

- 新しい用語を本文で初出させるときは、**先にここへ登録する**
- 定義文は**1文**にする。2文になったら、たとえ列に逃がす
- 同じ概念に複数の呼び方がある場合、**このテキストで使う1つ**を決めて書く
