---
title: "第3章 Dockerfile"
---

# 第3章 Dockerfile

> 🚧 **この章は執筆中です。**
> 以下は予定している章立てです。第2章を読み終えた方は、
> この章が書き上がるまでお待ちください。

## 予定している章立て

### 3.1 自分のアプリをイメージにする

- 3.1.1 なぜ必要か
- 3.1.2 Dockerfile とは

### 3.2 基本の命令

- 3.2.1 `FROM` — 土台を決める
- 3.2.2 `WORKDIR` — 作業ディレクトリ
- 3.2.3 `COPY` — ファイルを入れる
- 3.2.4 `RUN` — ビルド時にコマンドを実行
- 3.2.5 `CMD` — 起動時に実行するコマンド
- 3.2.6 `EXPOSE` と `ENV`
- 3.2.7 `CMD` と `ENTRYPOINT` の違い

### 3.3 ビルドして動かす

- 3.3.1 `docker build`
- 3.3.2 タグを付ける
- 3.3.3 ビルドしたイメージを起動する

### 3.4 レイヤとキャッシュ

- 3.4.1 1命令 = 1レイヤ
- 3.4.2 キャッシュが効く仕組み
- 3.4.3 依存インストールを先に書く理由
- 3.4.4 キャッシュを意図的に無効化する

### 3.5 dockerignore

- 3.5.1 `node_modules` を送らない
- 3.5.2 書き方
- 3.5.3 ビルドが遅いときはまずここ

### 3.6 FastAPI アプリをイメージにする

- 3.6.1 Dockerfile を書く
- 3.6.2 ビルドして起動する
- 3.6.3 動作確認

---

→ [第2章 インストールと基本操作](./02-install-and-basics.md) に戻る
