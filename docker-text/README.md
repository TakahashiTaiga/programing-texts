# プログラミング未経験から始める Docker

**このテキスト集の4冊目です。**

> 🚧 **準備中です。**
> [react-text](../react-text/README.md) → [python-text](../python-text/README.md)
> → [fastapi-text](../fastapi-text/README.md) の順で読んでください。

---

## この本の位置づけ

ここまでの3冊で、あなたは**3回の環境構築**を経験しています。

- Node.js を入れて、バージョンを確認して、PATH に悩んだ
- Python を入れて、venv を作って、pip でパッケージを入れた
- 「自分の環境では動くのに」という状況に、一度は遭遇したはず

**Docker は、この苦しみを根本から解決する道具です。**

この本を終えると、**コマンド1つでアプリ一式が起動する**状態が作れます。
5冊目の MySQL も、Docker があれば環境構築が一瞬で終わります。

> **なぜ4冊目なのか**
> Docker を最初に教えると「便利そうだけど、何が嬉しいのかわからない」で終わります。
> **3回の環境構築で苦労したあとだからこそ**、Docker の価値が実感できます。

## この本の前提

- **react-text と fastapi-text で、動くアプリを作った経験**
- ターミナルの基本操作

---

## 予定している目次

### 第0章 はじめに
- 0.1 このテキストについて
- 0.2 AI サポートの準備
- 0.3 この本で解決する問題

### 第1章 Docker が解決する問題
- 1.1 「自分の環境では動く」問題
- 1.2 仮想マシンとコンテナの違い
- 1.3 イメージとコンテナ
- 1.4 Docker を使うと何が変わるか

### 第2章 インストールと基本操作
- 2.1 Docker Desktop をインストールする
- 2.2 **よくあるトラブル**（WSL2、仮想化設定、Apple Silicon）
- 2.3 最初のコンテナを動かす
- 2.4 `run` / `ps` / `stop` / `rm`
- 2.5 `images` / `pull` / `rmi`
- 2.6 コンテナの中に入る（`exec`）
- 2.7 ログを見る（`logs`）

### 第3章 Dockerfile
- 3.1 自分のアプリをイメージにする
- 3.2 `FROM` / `WORKDIR` / `COPY` / `RUN` / `CMD`
- 3.3 ビルドする（`build`）
- 3.4 レイヤとキャッシュ
- 3.5 **キャッシュを効かせる書き順**
- 3.6 `.dockerignore`

### 第4章 ボリュームとネットワーク
- 4.1 コンテナを消すとデータが消える
- 4.2 バインドマウント（開発時の自動反映）
- 4.3 名前付きボリューム（データの永続化）
- 4.4 ポートを公開する
- 4.5 コンテナ同士を繋ぐ
- 4.6 **Windows の改行コード問題**

### 第5章 Docker Compose
- 5.1 コマンドが長くなりすぎる
- 5.2 `compose.yaml` の書き方
- 5.3 複数サービスを定義する
- 5.4 `up` / `down` / `logs` / `exec`
- 5.5 環境変数と `.env`
- 5.6 依存関係と起動順（`depends_on` / ヘルスチェック）

### 第6章 実践：React + FastAPI + MySQL
- 6.1 構成を考える
- 6.2 バックエンドをコンテナ化する
- 6.3 フロントエンドをコンテナ化する
- 6.4 MySQL を追加する
- 6.5 3つを繋ぐ
- 6.6 **`docker compose up` 一発で全部起動する**

### 第7章 イメージの最適化と本番運用
- 7.1 イメージが大きすぎる問題
- 7.2 マルチステージビルド
- 7.3 ベースイメージの選び方（alpine / slim）
- 7.4 セキュリティの基本
- 7.5 開発用と本番用を分ける

### 第8章 次のステップ
- 8.1 ここまでで身についたこと
- 8.2 次の本（MySQL）へ

### 解答編
