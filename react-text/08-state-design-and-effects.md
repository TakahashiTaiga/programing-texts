---
title: "第8章 状態設計と副作用"
---

# 第8章 状態設計と副作用

> 🚧 **この章は執筆中です。**
> 以下は予定している章立てです。

## この章で学ぶこと（予定）

- 複数のコンポーネントで state を共有できるようになる
- `useEffect` で、描画以外の処理を実行できるようになる
- サーバーからデータを取得して表示できるようになる
- 処理を自作のフックに切り出せるようになる

## この章の前提

- [第7章](./07-props-and-state.md) の props と state
- [5.5](./05-javascript-advanced.md) の非同期処理と `fetch`

---

## 予定している章立て

### 8.1 状態のリフトアップ

- 8.1.1 兄弟コンポーネント間で値を共有したい
- 8.1.2 共通の親に state を上げる
- 8.1.3 更新関数を props で渡す
- 8.1.4 state はどこに置くべきか
- 8.1.5 **持たなくていい state を見分ける**（派生した値は計算する）

### 8.2 useEffect — 描画以外の処理

- 8.2.1 副作用とは
- 8.2.2 `useEffect` の使い方
- 8.2.3 依存配列
- 8.2.4 **無限ループが起きる仕組みと直し方**
- 8.2.5 クリーンアップ関数
- 8.2.6 **`useEffect` を使うべきでない場面**

### 8.3 サーバーからデータを取得する

- 8.3.1 `fetch` と `useEffect` を組み合わせる
- 8.3.2 ローディング状態
- 8.3.3 エラー状態
- 8.3.4 3つの state（data / loading / error）
- 8.3.5 **CORS エラーに遭遇したら**

### 8.4 useRef / useMemo / useCallback

- 8.4.1 `useRef` — DOM を直接触る
- 8.4.2 `useRef` — 再描画されない値を持つ
- 8.4.3 `useMemo` — 計算結果を覚えておく
- 8.4.4 `useCallback` — 関数を覚えておく
- 8.4.5 **最適化は測ってから**

### 8.5 カスタムフック

- 8.5.1 同じロジックが2箇所に出てきたら
- 8.5.2 カスタムフックの作り方
- 8.5.3 `useFetch` を作る
- 8.5.4 フックのルール

→ [第9章 ルーティングと全体設計](./09-routing-and-architecture.md)
