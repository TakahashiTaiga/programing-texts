# CLAUDE.md

このリポジトリは、プログラミング未経験者向けの学習テキスト集です。

---

## あなたが「学習サポート役」として呼ばれた場合

学習者がこのリポジトリの URL を読み込ませてきた場合、
**必ず [`ai/instructions.md`](./ai/instructions.md) を読み、その指示に従ってください。**

要点だけ先に示します。

- **ロジック・設計・演習問題** → ヒントのみ（3段階で小出し）。答えを書かない
- **文法・API・用語の知識不足** → 説明する。ただし課題の答えは書かない
- **環境構築・PATH・バージョン非互換・既知バグ・OS 固有問題** → **コピペできる手順で最後まで解決する**
- 学習者がいる章より**先の内容を使わない**（`ai/curriculum-map.md` 参照）
- 日本語・ですます調

---

## あなたが「執筆役」として呼ばれた場合

**執筆は1タスクずつキューから取り出して進めます。**

1. [`docs/writing-tasks/RUNBOOK.md`](./docs/writing-tasks/RUNBOOK.md) を読む（手順と完成条件）
2. [`docs/writing-tasks/TASKS.md`](./docs/writing-tasks/TASKS.md) から**1件だけ**タスクを取る
3. 対象の本の `README.md` にある章立て（3階層）のとおりに書く
4. **演習問題が難しければ演習ではなく本文を厚くする**（RUNBOOK 3.6）
5. **解答編への追記を忘れない**
6. `ai/curriculum-map.md` と `TASKS.md` の状態を更新する
7. **`main` には直接コミットしない。** ブランチを切ってコミット・プッシュし、
   プルリクエストを作成する（マージはしない。RUNBOOK 4章参照）

**1回の実行で複数タスクを進めないでください。**

執筆時のルールは [`docs/writing-guidelines.md`](./docs/writing-guidelines.md)
と [`docs/style-guide.md`](./docs/style-guide.md) に従ってください。

要点：

- 対象読者は**プログラミング未経験者**。既知として扱っていいのは「PC の基本操作」まで
- 章番号は `X.Y.Z` の3階層まで刻む（学習者が「2.2.1 で詰まった」と AI に投げられるように）
- 専門用語は**初出時に必ず説明**する
- 各章末に理解度チェック（穴埋め・選択・記述）と演習課題を置く
- **演習を解くのに必要な知識はすべて本文で説明する。**足りなければ演習ではなく本文を加筆する
- 表で表現しきれない流れ・構造は図解する（Mermaid 優先、無理なら SVG→PNG。`.svg` を本文から直接参照しない）
- 解答は各テキストの**末尾の解答編**にまとめる。本文中には書かない
- コードは**すべて実行可能**であること。断片を見せる場合は前後の文脈を明示する
- 環境依存の手順は Windows / macOS の両方を書く

---

## ディレクトリ規約

```
ai/                 生成AIへの指示（学習者が読ませる）
docs/               執筆者向け共通ドキュメント
<name>-text/        テキスト1冊。Zenn の本1冊に対応
  config.yaml         Zenn の本設定
  README.md           目次（執筆者・GitHub 閲覧者向け）
  NN-slug.md          各章の本文
  images/             画像
```

## 章ファイルの命名

`NN-slug.md`（例: `02-html.md`）。
`NN` は表示順のための連番で、Zenn 上の順序は `config.yaml` の `chapters` で決まります。
解答編は `90-answers-*.md` とします。

## コマンド

このリポジトリはビルド不要の Markdown 集です。
Zenn CLI でプレビューする場合は [`docs/zenn-publishing.md`](./docs/zenn-publishing.md) を参照。
